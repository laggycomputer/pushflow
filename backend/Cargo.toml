[package]
name = "pushflow-server"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
hyper = { version = "1.6.0", features = ["client", "server", "http1"] }
hyper-util = { version = "0.1", features = ["full"] }
tokio = { version = "1", features = ["full"] }
http-body-util = "0.1.3"

[profile.release]
lto = "fat"